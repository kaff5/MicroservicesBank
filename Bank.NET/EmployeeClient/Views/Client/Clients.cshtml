@model List<EmployeeClient.ViewModels.UserViewModel>

@{
	ViewData["Title"] = "Пользователи банка";
}


<div class="accordion accordion-flush" id="accordionFlushExample">
	<div class="accordion-item">
		<h2 class="accordion-header" id="flush-headingOne">
			<button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false" aria-controls="flush-collapseOne">
				<h1 class="mb-3">Клиенты банка</h1>
			</button>
		</h2>
		<div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accordionFlushExample">
			<div class="accordion-body">

				<div class="card bg-light">
					@foreach (var user in Model)
					{
						@if (user.Role == "Client")
						{
							<a class="btn d-flex border-1 border mb-2 p-0 justify-content-between" style="border-color: #c6d2de" asp-action="ClientUser" asp-controller="Client" asp-route-userId="@user.UserId">
								<h5 class="p-1 ps-3">@user.Surname @user.Name @user.Patronymic</h5>

								@if (user.isBlocked)
								{
									<div class="bg-danger" style="height: 40px; width: 20px"></div>
								}
								else
								{
									<div class="" style="height: 40px; width: 20px; background-color: #3a9876"></div>
								}
							</a>
						}
					}
				</div>

			</div>
		</div>
	</div>
	<div class="accordion-item">
		<h2 class="accordion-header" id="flush-headingTwo">
			<button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseTwo" aria-expanded="false" aria-controls="flush-collapseTwo">
				<h1 class="mt-5">Сотрудники банка</h1>
			</button>
		</h2>
		<div id="flush-collapseTwo" class="accordion-collapse collapse" aria-labelledby="flush-headingTwo" data-bs-parent="#accordionFlushExample">
			<div class="accordion-body">
				<div class="card bg-light">

					@foreach (var user in Model)
					{
						@if (user.Role == "Employee")
						{
							<a class="btn d-flex border-1 border mb-2 p-0 justify-content-between" style="border-color: #c6d2de" asp-action="ClientUser" asp-controller="Client" asp-route-userId="@user.UserId">
								<h5 class="p-1 ps-3">@user.Surname @user.Name @user.Patronymic</h5>

								@if (user.isBlocked)
								{
									<div class="bg-danger" style="height: 40px; width: 20px"></div>
								}
								else
								{
									<div class="" style="height: 40px; width: 20px; background-color: #3a9876"></div>
								}
							</a>
						}
					}

				</div>
			</div>
		</div>
	</div>
</div>